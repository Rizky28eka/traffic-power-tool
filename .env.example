# Traffic Power Tool Environment Configuration

# Application Settings
NODE_ENV=production
SECRET_KEY=your-secret-key-here-minimum-32-characters-long
PORT=8000

# Database Configuration (Internal - tidak perlu diubah untuk single container)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=traffic_power_tool
POSTGRES_USER=traffic_user
POSTGRES_PASSWORD=traffic_password

# Redis Configuration (Internal - tidak perlu diubah untuk single container)
REDIS_HOST=localhost
REDIS_PORT=6379

# Application Configuration
PYTHONPATH=/app
PLAYWRIGHT_BROWSERS_PATH=/ms-playwright

# Optional: External Services
# GOOGLE_ANALYTICS_CREDENTIALS_PATH=/app/config/ga-credentials.json
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# Optional: Custom Configuration
# MAX_CONCURRENT_SESSIONS=100
# DEFAULT_TIMEOUT=60000
# LOG_LEVEL=INFO